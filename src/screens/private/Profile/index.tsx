import { SafeAreaView, Text } from 'react-native';

export function Profile() {
   return (
      <SafeAreaView className="flex-1 items-center justify-center bg-blue_dark-700">
         <Text className="text-white">Perfil do usuário</Text>
      </SafeAreaView>
   );
}
