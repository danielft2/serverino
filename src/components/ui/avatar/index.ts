import { AvatarButtonEdit } from './avatar-button-edit';
import { AvatarContainer } from './avatar-container';
import { AvatarFallback } from './avatar-fallback';
import { AvatarRoot } from './avatar-root';
import { AvatarUpdateOptions } from './avatar-update-options';

export const Avatar = {
  Root: AvatarRoot,
  Container: AvatarContainer,
  Fallback: AvatarFallback,
  ButtonEdit: AvatarButtonEdit,
  UpdateAvatar: AvatarUpdateOptions
};
